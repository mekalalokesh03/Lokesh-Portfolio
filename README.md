# Lokesh Portfolio

This is a personal portfolio website for Lokesh. It contains a static site with an `index.html`, `style.css`, and `script.js`, plus assets.

To run locally, open `index.html` in your browser.

Repository: https://github.com/mekalalokesh03/Lokesh-Portfolio

If you want this deployed with GitHub Pages, enable Pages in the repository settings or push the `gh-pages` branch (I pushed it for you).
